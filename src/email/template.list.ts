import * as path from 'path';

export const resetGiftEmailTemplatePath = path.join(
  __dirname,
  '/template/gift.email.ejs',
);

export const giftCertificateTemplatePath = path.join(
  __dirname,
  '/template/gift.certificate.ejs',
);
