version: '3'

services:
  blog:
    image: blog:latest
    container_name: blog
    restart: always
    networks:
      - web
    expose:
      - 80
    labels:
      - traefik.enable=true
      - traefik.backend=blog
      - traefik.docker.network=web
      - traefik.frontend.rule=Host:pojono.ru

networks:
  web:
    external:
      name: web
  default:
    driver: bridge